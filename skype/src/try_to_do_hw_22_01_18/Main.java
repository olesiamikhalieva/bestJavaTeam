//package try_to_do_hw_22_01_18;


import java.util.concurrent.ThreadLocalRandom;

//public class Main {
   // private static int counOfStudents = 40;

   // public static void main(String[] args) {



   // }
    //public static void createRandomStudents(ClassRoom nClass) {
       // for (int i = 0; i <= counOfStudents; ++i) {
   //         Student newStud = new Student();
   //         newStud.setAge(ThreadLocalRandom.current().nextInt(7, 16 + 1));
   //         newStud.setName("Student№" + ThreadLocalRandom.current().nextInt(0, 40 + 1));
      //      nClass.studToClass(newStud);
    //    }

  //  }

  //  private static void studFilter(ClassRoom classA, ClassRoom classB) {
        //for (Student student : initStudents.) {
   //         if (student.getAge() >= 7 && student.getAge() < 12) {
    //            classA.studToClass(student);
   //         } else if (student.getAge() >= 12) {
 //          }
  //      }
  //  }

 //   private static Student initStudents(int countOfStudents){
  //      Student[] studMass = new Student[countOfStudents];

 //   }
//}